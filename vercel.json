{
  "buildCommand": "pnpm --filter @workspace/focus-desk run build && REPO=$(git rev-parse --show-toplevel) && cp -r $REPO/artifacts/focus-desk/dist/public $REPO/public",
  "outputDirectory": "public",
  "installCommand": "pnpm install"
}
